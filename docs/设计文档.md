# keepBoard - 像素食人花桌面统计宠物

## 一、项目概述

keepBoard 是一款像素风格的桌面宠物程序，以"食人花"为主题形象，始终悬浮在屏幕顶部并自动吸附 Windows 任务栏。它不仅是可爱的桌面装饰，更是你工作习惯的数据记录者——实时统计键盘敲击、鼠标点击次数与屏幕使用时长，并通过生动的像素动画与你的每一次输入产生互动反馈。

---

## 二、核心需求扩展

### 2.1 基础功能需求

| 功能模块 | 详细需求 |
|---------|---------|
| **窗口置顶与吸附** | 1. 窗口始终置顶（TopMost），不被其他窗口遮挡<br>2. 启动后自动检测 Windows 任务栏位置（上/下/左/右），吸附到任务栏上方边缘<br>3. 支持拖拽移动，松开后自动吸附到最近的任务栏边缘<br>4. 任务栏位置变化时（如用户拖动任务栏），自动重新吸附 |
| **输入事件监听** | 1. 全局键盘钩子：监听所有按键按下事件（含组合键），区分按键类型（字母/数字/功能键/修饰键）<br>2. 全局鼠标钩子：监听左键、右键、中键点击事件，以及滚轮滚动事件<br>3. 低性能开销，钩子回调耗时 < 1ms |
| **数据统计** | 1. 键盘敲击总次数（按天/周/月/总计分组）<br>2. 键盘分类型统计（字母键、数字键、功能键、方向键、空格/回车等大键）<br>3. 鼠标左键/右键/中键点击次数<br>4. 滚轮滚动累计格数<br>5. 屏幕使用时长（基于系统空闲时间检测，连续无输入 > 30s 视为离开）<br>6. 实时按键速率（APM：每分钟按键数）<br>7. 峰值记录：单日最高敲击次数、最长连续使用时间等 |
| **食人花动画交互** | 1. **键盘敲击**：食人花脖子向上伸长 10~30px（随机），嘴巴张开→闭合做咬一下动作，持续 300ms<br>2. **快速连击**（>5 次/秒）：食人花疯狂摇头+连续咬合，进入"狂暴模式"<br>3. **鼠标点击**：食人花眨眼一次，头部轻微晃动<br>4. **滚轮滚动**：食人花叶子微微颤动<br>5. **空闲状态**：每 3~5 秒做一次呼吸式轻微起伏，偶尔眨眼<br>6. **长时间无输入**（>2 分钟）：食人花进入"睡眠"状态，闭合嘴巴+zzZ 气泡<br>7. **里程碑成就**（如敲击破 1000 次）：触发庆祝动画，头顶冒出彩色像素烟花 |
| **像素艺术风格** | 1. 所有精灵图采用 32x32 或 64x64 像素尺寸绘制<br>2. 调色板：复古 FC 风格 16 色调色板（绿/红/黄为主色调）<br>3. 像素边缘锐利，无抗锯齿，保持"块状感"<br>4. 字体：等宽像素字体（如 Press Start 2P / Zpix / 传统宋体 12px）<br>5. UI 边框：双线像素描边 |

### 2.2 扩展功能建议（可选二期实现）

- 🎵 **音效反馈**：咬合时播放 8-bit 音效，可开关
- 🌱 **成长系统**：累计敲击次数达到阈值后食人花"升级"（长大、变色、长出新叶子）
- 🎯 **每日目标**：设定每日敲击目标，达成时触发特殊动画
- 📊 **数据导出**：将统计数据导出为 CSV / JSON
- ☁️ **云同步**：多设备数据同步（需后端支持）
- 🎨 **皮肤切换**：食人花不同配色 / 其他像素宠物（仙人掌、史莱姆、猫咪）
- 🏆 **成就系统**：解锁 50+ 成就徽章，每个解锁触发专属动画
- 🔔 **休息提醒**：连续使用超过 1 小时弹窗提醒起身活动
- 📝 **热图**：键盘热力图，显示哪个按键被敲击最多

---

## 三、技术选型建议

### 3.1 桌面框架选型对比

| 方案 | 优势 | 劣势 | 推荐指数 |
|-----|-----|-----|---------|
| **方案 A：Electron + Node.js** | 生态成熟，npm 包丰富；全局钩子库（iohook、uiohook-napi）易用；Canvas/SVG 像素渲染方便；打包工具完善（electron-builder） | 包体积大（~100MB+）；内存占用较高（~200MB） | ⭐⭐⭐⭐ |
| **方案 B：Tauri + Rust** | 包体积极小（~5-10MB）；内存占用低（~30MB）；Rust 写全局钩子性能极佳；原生调用 Windows API 方便 | Rust 学习曲线陡；生态不如 Electron 成熟；前端仍需 HTML/CSS/JS | ⭐⭐⭐⭐⭐ **推荐** |
| **方案 C：PyQt5/PySide6 + Python** | Python 开发快；pynput / keyboard 库全局监听成熟；PyQt 图形能力强 | 打包后体积也不小；运行效率一般；风格与像素风需额外定制 | ⭐⭐⭐ |
| **方案 D：WinUI 3 / WPF + C#** | 原生 Windows 体验；性能好；Win32 API 调用无缝 | 跨平台能力弱；.NET 运行时依赖；像素动画渲染较繁琐 | ⭐⭐⭐ |
| **方案 E：C++ + Qt / Dear ImGui** | 性能最佳；体积可控；完全原生 | 开发效率低；代码量大；调试难度高 | ⭐⭐ |

### 3.2 推荐方案：**Tauri 2.0 + Rust + 前端 Canvas**

**核心理由：**
1. **轻量**：5-10MB 安装包，内存占用仅为 Electron 的 1/5~1/8
2. **性能**：Rust 编写全局键盘/鼠标钩子，回调零延迟
3. **像素渲染**：前端使用 HTML5 Canvas，像素级操控完美匹配像素风
4. **原生能力**：Tauri 调用 Windows API（任务栏位置检测、窗口置顶）非常方便

### 3.3 前端技术栈建议

| 层 | 技术选择 | 说明 |
|----|---------|-----|
| 框架 | **React 18** 或 **Vue 3** 或 **纯原生 JS** | 项目较小，纯原生 JS + Canvas 也完全可行，减少依赖 |
| 像素渲染 | **HTML5 Canvas 2D** + `imageRendering: pixelated` | 关闭抗锯齿，保证像素锐利 |
| 动画 | **requestAnimationFrame** 手写帧动画循环 | 精确控制 60fps / 30fps 帧率 |
| 状态管理 | 轻量方案：Zustand / Pinia / 原生 localStorage | 数据量小，无需复杂方案 |
| 样式 | **Tailwind CSS** 或手写 CSS | 像素风 UI 样式量不大 |
| 打包 | Tauri 内置打包器（WiX / NSIS） | 生成 .msi 或 .exe 安装包 |

### 3.4 全局输入监听方案

| 方案 | 说明 |
|-----|-----|
| **Tauri 插件** | `tauri-plugin-global-shortcut`（仅快捷键，不够用） |
| **Rust 库：`rdev`** ⭐ 推荐 | 跨平台全局输入监听，支持键盘、鼠标、滚轮事件，API 简洁 |
| **Rust 库：`inputbot`** | 仅 Windows/Linux，更底层 |
| **Windows API 直接调用** | `SetWindowsHookEx` 安装 `WH_KEYBOARD_LL` 和 `WH_MOUSE_LL` 钩子，最灵活但代码多 |

### 3.5 数据持久化方案

- **开发阶段**：Tauri 插件 `tauri-plugin-store`（JSON 文件存储，API 简单）
- **数据量大时**：`tauri-plugin-sql` + SQLite，支持复杂查询与历史趋势
- **文件位置**：Windows 下 `%APPDATA%/keepBoard/data.db`

### 3.6 像素素材绘制工具建议

| 工具 | 说明 |
|-----|-----|
| **Aseprite** | 像素画行业标准，动画帧时间轴功能强大，可导出精灵表（Spritesheet）+ JSON 元数据 |
| **Piskel** | 免费开源，浏览器即用，适合快速绘制 |
| **LibreSprite** | Aseprite 的开源分支，功能完整 |

---

## 四、系统架构设计

### 4.1 分层架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      前端 UI 层 (WebView)                    │
│  ┌───────────────┐  ┌───────────────┐  ┌──────────────────┐ │
│  │  食人花动画    │  │  统计面板      │  │  设置弹窗         │ │
│  │  Canvas 渲染   │  │  DOM 展示      │  │  DOM 表单        │ │
│  └───────┬───────┘  └───────┬───────┘  └────────┬─────────┘ │
│          │                  │                   │           │
│  ┌───────┴──────────────────┴───────────────────┴─────────┐ │
│  │              状态管理层 (Zustand / Store)              │ │
│  │   countStore │ animStore │ settingStore │ statsStore    │ │
│  └───────────────────────┬────────────────────────────────┘ │
└──────────────────────────┼──────────────────────────────────┘
                           │ IPC 调用 (Tauri invoke/listen)
┌──────────────────────────┼──────────────────────────────────┐
│                     Rust 后端层 (Core)                      │
│  ┌────────────────┐ ┌───────────────┐ ┌───────────────────┐ │
│  │ 全局输入钩子    │ │ 窗口管理模块   │ │  数据持久化模块    │ │
│  │ (rdev Hook)    │ │ (置顶/吸附)    │ │  (Store/SQLite)   │ │
│  └───────┬────────┘ └───────────────┘ └────────┬──────────┘ │
│          │                                      │            │
│  ┌───────┴──────────────────────────────────────┴──────────┐ │
│  │                   事件总线 / 调度器                      │ │
│  │  KeyboardEvent → 统计→动画  MouseEvent → 统计→动画       │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 核心数据流

```
用户按键/点击
    │
    ▼
[Rust: rdev 全局钩子] ──(原始事件)──► [Rust: 统计累加器]
    │                                       │
    │                                       ├──► 更新今日统计数据
    │                                       └──► 写入持久化存储
    │
    └──(IPC emit: input-event)──► [前端: 动画控制器]
                                         │
                                         ├──► 判断事件类型（键击/鼠标点击/滚轮）
                                         ├──► 计算动画参数（伸长高度、咬合速度）
                                         └──► 触发 Canvas 帧动画（requestAnimationFrame）
```

### 4.3 关键模块设计

#### 4.3.1 窗口管理模块

```rust
// Rust 伪代码
struct WindowManager {
    window: tauri::Window,
    taskbar_rect: Rect,    // 任务栏矩形区域
    dock_margin: i32,      // 吸附间距（像素）
}

impl WindowManager {
    fn init(window: tauri::Window) -> Self { ... }
    
    // 1. 通过 Win32 API (FindWindow + GetWindowRect) 读取任务栏位置
    fn detect_taskbar(&self) -> Rect { ... }
    
    // 2. 计算吸附位置（默认吸附在任务栏上方居中）
    fn calc_dock_position(&self, drag_pos: Option<Point>) -> Point { ... }
    
    // 3. 移动窗口 + 设置始终置顶
    fn apply_dock(&self, pos: Point) { ... }
    
    // 4. 监听任务栏位置变更消息（WM_SETTINGCHANGE）
    fn on_taskbar_changed(&mut self) { ... }
}
```

#### 4.3.2 食人花动画状态机

```typescript
// 前端 TypeScript 伪代码
type AnimState = 
  | { name: 'idle';        since: number }      // 待机呼吸
  | { name: 'sleep';       since: number }      // 睡眠 zzZ
  | { name: 'bite';        start: number; intensity: number }  // 咬合动作
  | { name: 'frenzy';      start: number; hits: number }       // 狂暴连击
  | { name: 'celebrate';   start: number; type: string };      // 庆祝

class PiranhaAnimator {
  private state: AnimState = { name: 'idle', since: Date.now() };
  private ctx: CanvasRenderingContext2D;
  private sprites: SpriteSheet;  // 加载的像素精灵图
  
  // 每一帧被 requestAnimationFrame 调用
  tick(now: number) {
    // 1. 根据当前 state 计算时间进度
    // 2. 绘制食人花各部件（茎、叶子、头、嘴、眼睛）
    // 3. 应用插值动画（伸长距离 t ∈ [0,1] → 贝塞尔缓动）
    // 4. 叠加粒子效果（庆祝时的像素烟花）
  }
  
  onKeyPress(keyType: KeyType) {
    // 切换到 bite 状态，随机强度
    this.state = { name: 'bite', start: Date.now(), intensity: Math.random() * 0.5 + 0.5 };
  }
  
  onComboThresholdReached() {
    this.state = { name: 'frenzy', start: Date.now(), hits: 0 };
  }
  
  onIdleTimeout() {
    this.state = { name: 'sleep', since: Date.now() };
  }
}
```

#### 4.3.3 数据统计结构

```typescript
interface DailyStats {
  date: string;                 // YYYY-MM-DD
  keyboardTotal: number;        // 总键击数
  keyboardByType: {             // 按键类型细分
    alpha: number;
    numeric: number;
    function: number;
    modifier: number;
    arrow: number;
    bigKey: number;             // 空格/回车/退格
    other: number;
  };
  mouseLeft: number;            // 左键
  mouseRight: number;           // 右键
  mouseMiddle: number;          // 中键
  wheelScrolls: number;         // 滚轮滚动次数
  screenTimeMs: number;         // 屏幕使用时长（毫秒）
  peakApm: number;              // 峰值 APM
  longestSessionMs: number;     // 最长连续会话
}

interface LifetimeStats {
  totalKeystrokes: number;
  totalClicks: number;
  totalScreenTimeMs: number;
  achievements: string[];       // 已解锁成就 ID 列表
  level: number;                // 食人花等级
}
```

---

## 五、像素艺术设计规范

### 5.1 食人花角色设计（64x64 像素）

```
各部件分层（便于动画独立控制）：
┌──────────────────────────────────────────────────┐
│ Layer 4: 嘴部（分开/闭合两帧 + 牙齿）             │
│ Layer 3: 头部（眼睛 + 脸颊 + 花萼）               │
│ Layer 2: 茎秆（可弹性变形，存储为 3-5 段控制点）   │
│ Layer 1: 叶子（左右各一片，轻微独立摆动）          │
│ Layer 0: 花盆（不动，品牌 LOGO 位置）             │
└──────────────────────────────────────────────────┘
```

### 5.2 调色板（16 色复古像素风）

| 色号 | HEX | 用途 |
|-----|-----|-----|
| 0 | `#1A1C2C` | 背景深色 / 外描边 |
| 1 | `#2E3446` | 内描边 |
| 2 | `#455563` | 阴影 |
| 3 | `#6B7A8A` | 中性灰 |
| 4 | `#9FB0C2` | 亮灰 |
| 5 | `#D8E2EC` | 高光白 |
| 6 | `#2D5A27` | 深绿（茎阴影） |
| 7 | `#4A8F3C` | 中绿（茎主色） |
| 8 | `#7BC95A` | 亮绿（叶子主色） |
| 9 | `#A8E063` | 嫩绿（高光） |
| 10 | `#C23B22` | 深红（花瓣阴影） |
| 11 | `#E85D3A` | 红色（花瓣主色） |
| 12 | `#FF8C42` | 橙红（高光） |
| 13 | `#FFD93D` | 黄色（牙齿/花蕊） |
| 14 | `#FFFFFF` | 纯白 |
| 15 | `#000000` | 纯黑 |

### 5.3 动画帧率

- 待机呼吸：**12 fps**（0.083s/帧）
- 咬合动作：**30 fps**（0.033s/帧），共 6~8 帧
- 狂暴模式：**24 fps**，每帧随机微抖
- 庆祝烟花：**20 fps**，粒子系统

---

## 六、开发里程碑建议

| 阶段 | 周期 | 交付物 |
|-----|-----|-------|
| **M1：环境搭建 + 基础窗口** | 3 天 | Tauri 项目初始化；窗口透明+无边框+置顶；任务栏位置检测与自动吸附 |
| **M2：全局输入 + 基础统计** | 3 天 | Rust rdev 钩子接入；键击/点击/滚轮计数；屏幕使用时长；数据持久化到本地 |
| **M3：食人花像素画 + 基础动画** | 5 天 | 绘制食人花像素精灵图；Canvas 渲染器；待机/咬合/睡眠三种状态机 |
| **M4：扩展交互 + 统计面板** | 4 天 | 狂暴模式、连击检测、庆祝动画；统计面板 UI（今日/总计切换）；成就系统骨架 |
| **M5：打磨 + 打包发布** | 3 天 | 音效、设置弹窗、数据导出；WiX 打包安装程序；性能优化（内存 < 80MB） |
| **总计** | **~18 天** | |

---

## 七、风险与注意事项

| 风险点 | 应对方案 |
|-------|---------|
| **Windows 全局钩子性能** | 使用 Rust `rdev` 底层实现，回调函数内只做最小逻辑（发出事件 + 原子计数），复杂逻辑丢给另一线程 |
| **反作弊软件误报** | 钩子行为可能被某些游戏反作弊拦截 → 文档中注明游戏时建议暂停；或提供"暂停监听"快捷键 |
| **Canvas 像素渲染模糊** | 设置 `canvas.style.imageRendering = 'pixelated'` + 画布尺寸 = CSS 尺寸 × devicePixelRatio，后 `ctx.scale(dpr, dpr)` |
| **Tauri WebView2 依赖** | 大多数 Win10/11 已自带；打包时可选内置 WebView2 加载器（体积增加 ~15MB） |
| **任务栏自动隐藏模式** | 检测 `ABM_GETSTATE` 返回值，若自动隐藏则吸附时预留 2px 触发区，避免遮挡任务栏唤起 |

---

## 八、后续可拓展方向（脑洞）

1. **多人联动**：好友之间看到对方的食人花，互相"投喂"（发一个敲击事件让对方的花咬一下）
2. **打字游戏模式**：限时打字，食人花根据正确率做不同表情
3. **与番茄钟结合**：专注时间内食人花茁壮成长，分心时它会难过
4. **键盘皮肤映射**：统计数据驱动像素化键盘图，按键越热颜色越红
5. **天气联动**：根据天气 API，下雨天花盆里长蘑菇，晴天开小花
